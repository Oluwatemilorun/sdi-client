export default async ({ store }) => {
	// let step = path.split('/');
	// let breadcrumbs = [];

	// step.forEach((view, index) => {
	// 	if (index == 0) {
	// 		breadcrumbs.push({
	// 			text: "home", url: "/"
	// 		});
	// 	}
	// 	else {
	// 		let url = '';
	// 		let viewComponent = router.getMatchedComponents(view);
	// 		for (let i = 1; i <= index; i++) {
	// 			url += `/${step[i]}`;
	// 		}
	// 		breadcrumbs.push({
	// 			text: viewComponent.name || view,
	// 			url
	// 		});
	// 	}
	// });

	// store.commit('setBreadcrumbs', breadcrumbs);
}